services:
  - type: web
    name: jarvis-web-ui
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 app:app
    envVars:
      - key: PORT
        value: 10000
